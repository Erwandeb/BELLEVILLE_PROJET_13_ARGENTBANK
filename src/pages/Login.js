import React from 'react';
import Navigation from '../components/Navigation';

const Login = () => {
    return (
        <div className="login">
            <Navigation/>
        </div>
    );
};

export default Login;